type Collection @entity {
	id: ID!
	address: Bytes!
	type: String!
}

type Token @entity {
	id: ID!
	address: Bytes!
	owner: User!
	tokenId: BigInt!
}

type User @entity {
  id: ID!
  tokens: [Token!]! @derivedFrom(field: "owner")
}