type Collection @entity {
	id: ID!
	address: Bytes!
	type: String!
  	issuer: Bytes!
}

type Token @entity {
	id: ID!
	address: Bytes!
	tokenId: BigInt!
	owner: User!
}

type User @entity {
  id: ID!
  tokens: [Token!]! @derivedFrom(field: "owner")
}